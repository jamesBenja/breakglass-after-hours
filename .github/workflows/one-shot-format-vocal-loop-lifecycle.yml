name: One-shot format Vocal loop lifecycle test

on:
  push:
    branches:
      - fix/vocal-loop-live-scrub-lifecycle
    paths:
      - .github/workflows/one-shot-format-vocal-loop-lifecycle.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout fix branch
        uses: actions/checkout@v4
        with:
          ref: fix/vocal-loop-live-scrub-lifecycle

      - name: Use Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Format lifecycle regression test
        run: pnpm exec prettier --write tests/spectra-fader-routing.test.js

      - name: Remove one-shot workflow
        run: rm .github/workflows/one-shot-format-vocal-loop-lifecycle.yml

      - name: Commit canonical formatting
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tests/spectra-fader-routing.test.js .github/workflows/one-shot-format-vocal-loop-lifecycle.yml
          git commit -m "Apply canonical formatting to Vocal lifecycle test"
          git push origin HEAD:fix/vocal-loop-live-scrub-lifecycle
