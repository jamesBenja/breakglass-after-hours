name: One-shot format Vocal route tests

on:
  push:
    branches:
      - fix/vocal-playback-route-recovery
    paths:
      - .github/workflows/one-shot-format-vocal-route.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout fix branch
        uses: actions/checkout@v4
        with:
          ref: fix/vocal-playback-route-recovery

      - name: Use Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Format Vocal route tests exactly
        run: pnpm exec prettier --write tests/spectra-fader-routing.test.js tests/spectra-vocal-input.test.js

      - name: Remove one-shot workflow
        run: rm .github/workflows/one-shot-format-vocal-route.yml

      - name: Commit canonical formatting
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tests/spectra-fader-routing.test.js tests/spectra-vocal-input.test.js .github/workflows/one-shot-format-vocal-route.yml
          git commit -m "Apply canonical formatting to Vocal route tests"
          git push origin HEAD:fix/vocal-playback-route-recovery
