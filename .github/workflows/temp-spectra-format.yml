name: Temporary Spectra Formatter

on:
  push:
    branches:
      - fix/spectra-navigation-guitar-recording

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/spectra-navigation-guitar-recording
      - uses: pnpm/action-setup@v4
        with:
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec prettier --write src/gameplay/StudioLoopEnhancements.js
      - name: Commit formatting
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git diff --quiet; then
            echo "No formatting changes"
          else
            git add src/gameplay/StudioLoopEnhancements.js
            git commit -m "Format Spectra navigation changes"
            git push origin HEAD:fix/spectra-navigation-guitar-recording
          fi
